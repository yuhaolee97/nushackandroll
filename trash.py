     
        
        # # print(top_4_dictionaries[i])
        # r = requests.get(google_url + 'origins = ' + str(key.latitude) + ',' + str(key.longitude) + 
        #             '&destinations = ' + str(average[0]) + ',' + str(average[1]) + 
        #             '&key = ' + api_key) 
                     
    # json method of response object 
    # return json format result 
        # x = r.json() 
    
    # # by default driving mode considered 
    
    # # print the value of x 
        # print(x) 

        # import googlemaps
        # gmaps = googlemaps.Client(key=api_key)
        # origins = []
        # destinations = []
        # for i in range (0,15):
        # matrix = gmaps.distance_matrix(origins, destinations, mode="bicycling")
        # print matrix



    # new_data = pd.DataFrame({"Longitude":[average[1]], 
    #                      "Latitude":[average[0]]})
    # centroid = km.predict(new_data)